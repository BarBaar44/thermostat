# DEPRECATED
#
# This monolithic automation has been split into three separate automations
# plus two shared scripts, for better isolation, traceability, and trace
# history retention. See README.md for the full writeup of why and what
# changed.
#
# Replacement files:
#   automations/trv_setpoint_sync.yaml
#   automations/heating_schedule.yaml
#   automations/garden_door_heating_pause.yaml
#   scripts/pause_livingroom_heating.yaml
#   scripts/resume_livingroom_heating.yaml
#
# Action required in Home Assistant: delete/disable the old "House warming"
# automation and import the three files above (plus the two scripts) in its
# place. This file is kept only for historical reference and git blame, and
# is no longer maintained.
